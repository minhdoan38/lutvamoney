interface:
  display_name: "Copywriting - Rylai"
  short_description: "Use this skill when writing headlines, landing page copy, CTA..."
  default_prompt: "Use $copywriting to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
