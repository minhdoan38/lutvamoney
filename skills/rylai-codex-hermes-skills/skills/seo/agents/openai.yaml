interface:
  display_name: "Seo - Rylai"
  short_description: "Perform an evidence-led SEO audit using accessible pages, sou..."
  default_prompt: "Use $seo to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
