interface:
  display_name: "Finance Skills - Rylai"
  short_description: "Route finance requests to the capabilities actually available..."
  default_prompt: "Use $finance-skills to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
