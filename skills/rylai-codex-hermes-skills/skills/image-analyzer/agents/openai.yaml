interface:
  display_name: "Image Analyzer - Rylai"
  short_description: "Analyze local or supplied images with native Codex or Hermes..."
  default_prompt: "Use $image-analyzer to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
