interface:
  display_name: "Content Analysis - Rylai"
  short_description: "Content extraction and analysis for video, podcast, article,..."
  default_prompt: "Use $content-analysis to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
