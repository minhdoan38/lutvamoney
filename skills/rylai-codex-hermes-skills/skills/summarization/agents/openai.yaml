interface:
  display_name: "Summarization - Rylai"
  short_description: "Summarize documents, articles, conversations, code, and techn..."
  default_prompt: "Use $summarization to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
