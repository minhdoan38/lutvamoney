interface:
  display_name: "Creative Writing - Rylai"
  short_description: "Plan, draft, revise, and audit creative or long-form writing..."
  default_prompt: "Use $creative-writing to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
