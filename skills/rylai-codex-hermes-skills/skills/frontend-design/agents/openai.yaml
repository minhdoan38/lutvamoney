interface:
  display_name: "Frontend Design - Rylai"
  short_description: "Build and visually verify accessible production web interfaces."
  default_prompt: "Use $frontend-design to implement this interface in the existing project, complete responsive and accessibility states, and verify it in a browser."
policy:
  allow_implicit_invocation: true
