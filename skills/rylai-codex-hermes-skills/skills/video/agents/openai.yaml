interface:
  display_name: "Video - Rylai"
  short_description: "When the user wants to create, generate, or produce video con..."
  default_prompt: "Use $video to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
