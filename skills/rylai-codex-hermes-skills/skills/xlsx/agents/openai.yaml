interface:
  display_name: "XLSX by Rylai"
  short_description: "Create, edit, inspect, and verify Excel workbooks."
  default_prompt: "Use $xlsx to create or edit this workbook with the Rylai workflow, preserve the source, and verify formulas and saved output."
policy:
  allow_implicit_invocation: true
