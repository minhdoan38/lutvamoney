interface:
  display_name: "Video Generation - Rylai"
  short_description: "Generate videos through the native or configured video backen..."
  default_prompt: "Use $video-generation to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
