interface:
  display_name: "Netlify Deploy - Rylai"
  short_description: "Deploy web projects to Netlify using the Netlify CLI (`npx ne..."
  default_prompt: "Use $netlify-deploy to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
