interface:
  display_name: "PDF by Rylai"
  short_description: "Create, inspect, edit, extract, and verify local PDF files."
  default_prompt: "Use $pdf to complete this PDF task with local open-source tools and verify the output."
policy:
  allow_implicit_invocation: true
