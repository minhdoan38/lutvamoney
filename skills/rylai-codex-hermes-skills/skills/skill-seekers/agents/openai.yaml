interface:
  display_name: "Skill Seekers - Rylai"
  short_description: "Convert documentation websites, GitHub repositories, and PDFs..."
  default_prompt: "Use $skill-seekers to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
