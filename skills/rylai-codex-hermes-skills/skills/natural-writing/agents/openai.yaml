interface:
  display_name: "Natural Writing - Rylai"
  short_description: "Write like a human, not a language model"
  default_prompt: "Use $natural-writing to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
