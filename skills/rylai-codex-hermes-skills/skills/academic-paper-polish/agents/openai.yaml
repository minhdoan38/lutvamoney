interface:
  display_name: "Academic Paper Polish - Rylai"
  short_description: "Polish academic and research writing for clarity, structure,..."
  default_prompt: "Use $academic-paper-polish to handle this request with its portable workflow, then verify all required tools and dependencies."
policy:
  allow_implicit_invocation: true
